# ---------------------------------------------------------------------------
# Spinify – copy to .env and fill in real values
# ---------------------------------------------------------------------------

# API server
SPINIFY_API_HOST=0.0.0.0
SPINIFY_API_PORT=8000

# Hardware: 0 = use real RC522/GPIO on Pi, 1 = simulate (for dev without Pi)
SPINIFY_SIMULATE_HARDWARE=0

# Spotify (from https://developer.spotify.com/dashboard)
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# Where Spotify redirects after login (use Pi IP if you open the UI from another device)
SPOTIFY_REDIRECT_URI=http://localhost:8000/api/spotify/callback

# Where to send the user after OAuth (your web UI URL)
SPINIFY_WEB_ORIGIN=http://localhost:5173

# ---------------------------------------------------------------------------
# Optional (defaults are usually fine)
# ---------------------------------------------------------------------------
# Turntable steps per revolution (half-steps). Default: 4096.
# SPINIFY_STEPS_PER_REV=4096
# Tone-arm steps per revolution (half-steps). Default: 512 (measured on your hardware).
# SPINIFY_TONE_ARM_STEPS_PER_REV=512
# Max allowed tone-arm travel in steps from home (to limit arc). Default: 512.
# SPINIFY_TONE_ARM_MAX_STEPS=512
# Target RPM for turntable when spinning. Default: 33.33.
# SPINIFY_TURNTABLE_RPM=33.33
